!!!
%html{lang: 'en'}
  %head
    %meta{charset: 'utf-8'}/
    %meta{content: 'Take control of your multiscreen world', name: 'description'}/
    %meta{content: 'Agile Freaks', name: 'author'}/
    %title= content_for?(:title) ? yield(:title) : 'Omnipaste - Take control of your multi-screen world'
    = favicon_link_tag
    = stylesheet_link_tag 'embeddable', :media => 'all'
    = csrf_meta_tag

  %body.embeddable
    %header.header__meta
      .header-container
        .container
          = render partial: 'layouts/embeddable/header'
    .main.main--center-content
      .container
        = yield
      - if flash[:error].present?
        .error
          %p Could not complete the request
          %p The server returned the following message:
          %p
            = flash[:error]
  = javascript_include_tag :embeddable